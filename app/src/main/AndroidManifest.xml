<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mem"
        tools:targetApi="31">
        <activity
            android:name=".main.personalinformation.PersonalInformationActivity"
            android:exported="false" />
        <activity
            android:name=".main.verify.VerifyActivity"
            android:exported="false" />
        <activity
            android:name=".main.register.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".main.login.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".main.splash.SplashActivity"
            android:exported="false" />
        <activity
            android:name=".main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>